<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://www.mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <context id="default" targetRuntime="Mybatis3">
        <commentGenerator>
            <!--是否去掉自动生成的时间和注释-->
            <!--默认
            <property name="suppressDate" value="false"/>
            <property name="suppressAllComments" value="false"/>
            -->
            <property name="suppressDate" value="true"/>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>


        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://81.68.114.87:3306/mysql?useUnicode=true&characterEncoding=utf8" userId="root"
                        password="sy19820426"></jdbcConnection>

        <!--<jdbcConnection driverClass="oracle.jdbc.driver.OracleDriver"
                        connectionURL="jdbc:oracle:thin:@110.87.100.158:13521:lims" userId="lims_data"
                        password="limslims"></jdbcConnection> -->
        <!-- 对decimal的转换，true为big decimal，false为integer，默认是false -->
        <javaTypeResolver>
            <property name="forceBigDecimals" value="false"/>
        </javaTypeResolver>

        <!--pojo的生成位置 -->
        <javaModelGenerator targetPackage="cn.besbing.entities.slave" targetProject="src/main/java">
            <!-- 是否让schema做为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
            <!-- 从数据库返回的值清理空格 -->
            <property name="trimStirng" value="true"/>
        </javaModelGenerator>

        <!-- 生成映射文件(mapper.xml)的包名和位置 -->
        <sqlMapGenerator targetPackage="mappers.slave" targetProject="src/main/resources">
            <!-- 是否让schema做为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
        </sqlMapGenerator>

        <!-- 生成dao类的包名和位置(mapper的接口) -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="cn.besbing.mapper.slave" targetProject="src/main/java" >
            <!-- 是否让schema做为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
        </javaClientGenerator>

        <!-- 生成对应的表名及类名 -->
        <table tableName="report_sign_record" />

        <!-- 有些表的字段需要指定java类型
         <table schema="" tableName="">
            <columnOverride column="" javaType="" />
        </table> -->
    </context>
</generatorConfiguration>